# Database Configuration (SECURE - No passwords in version control)
DB_HOST=localhost
DB_PORT=3306
DB_USER=your_database_username
# DB_PASSWORD should be set via environment variable or secure secret management
DB_NAME=dbms_self_healing

# Security Configuration (CHANGE THESE IN PRODUCTION)
JWT_SECRET_KEY=your-jwt-secret-key-change-in-production-use-256-bit-key
API_KEY=your-api-key-change-in-production-use-strong-random-key

# API Configuration
API_HOST=0.0.0.0
API_PORT=8000
DEBUG=False

# CORS Configuration (for frontend integration)
FRONTEND_URL=http://localhost:3000