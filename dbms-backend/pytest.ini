[pytest]
# Ignore the unused scenarios directory to prevent collection errors
# as they contain relative imports that fail when not run as part of the demo runner
norecursedirs = demo_tests/scenarios/unused
python_files = test_*.py *_test.py